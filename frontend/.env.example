# Salin file ini ke .env dan sesuaikan dengan alamat backend kamu
VITE_API_BASE_URL=http://localhost:8080

# Kunci publik VAPID untuk push notification (harus sama dengan VAPID_PUBLIC_KEY di backend/.env)
# Generate dengan: node backend/scripts/generate-vapid-keys.js
VITE_VAPID_PUBLIC_KEY=
